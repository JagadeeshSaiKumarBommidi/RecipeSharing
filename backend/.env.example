# Database Configuration
# Replace the placeholder below with your MongoDB connection string (Atlas or local).
# Example (Atlas): mongodb+srv://<username>:<password>@cluster0.mongodb.net/recipesharing?retryWrites=true&w=majority
MONGODB_URI=<your-mongodb-connection-string>

# Server Configuration
# Database Configuration
# Replace the placeholder below with your MongoDB connection string (Atlas or local).
# Example (Atlas): mongodb+srv://<username>:<password>@cluster0.mongodb.net/recipesharing?retryWrites=true&w=majority
MONGODB_URI=<your-mongodb-connection-string>

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration
# Use a strong random secret. Generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=<your-jwt-secret>

# Client URL (for CORS)
CLIENT_URL=http://localhost:5173
